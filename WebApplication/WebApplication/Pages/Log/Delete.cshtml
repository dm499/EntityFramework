@page
@model WebApplication.Pages.Log.DeleteModel

@{
    ViewData["Title"] = "Delete Log";
}

    <div class="border container" style="padding:30px;">
        <div class="alert alert-danger" role="alert">
            Are you sure you want to delete this?
        </div>
        <table class="table table-striped">
            <thead>
                <tr>
                    <th scope="col">@Html.DisplayNameFor(model => model.TransactionLogs.Credit)</th>
                    <th scope="col">@Html.DisplayNameFor(model => model.TransactionLogs.Balance)</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>@Html.DisplayFor(model => model.TransactionLogs.Credit)</td>
                    <td>@Html.DisplayFor(model => model.TransactionLogs.Balance)</td>
                </tr>
            </tbody>
        </table>
        <form method="post">
            <div class="form-group row mt-5">
                <div class="col-2">
                    <input type="hidden" asp-for="TransactionLogs.Id" />
                    <input type="submit" value="Delete" class="btn btn-outline-danger form-control" />
                </div>
                <div class="col-2">
                    <a asp-page="./Index" class="btn btn-outline-secondary">Back</a>
                </div>
            </div>
        </form>
    </div>
